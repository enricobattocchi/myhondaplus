---
name: Release notes format
description: GitHub release descriptions should always start with "## What's new"
type: feedback
---

Release descriptions should always start with `## What's new` for consistency across all releases.

**Why:** User explicitly asked for consistency in release descriptions.
**How to apply:** When creating GitHub releases, always use `## What's new` as the first heading.
